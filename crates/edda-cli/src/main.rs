//! Instance administration CLI. 
//! Not implemented yet, but will eventually provide a thin binary that talks to
//! `edda-db`/`edda-auth` directly, bypassing HTTP entirely — instance administration (`edda-cli user create/list/disable/delete`) run from the same host/container as

#[tokio::main]
async fn main() {
    println!("edda-cli: not yet implemented");
}
