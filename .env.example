# Edda environment configuration reference.
#
# NOTE on how this file actually gets used: Edda's own binary does NOT load
# `.env` at runtime — only `sqlx`'s build-time query-checking macros do (via
# `dotenvy`, a transitive dependency of `sqlx-macros-core`; confirmed with
# `cargo tree -i dotenvy`), and only for DATABASE_URL/SQLX_OFFLINE below.
# Every other variable here must be set in your actual shell/service
# environment (`export VAR=...`, `docker run -e VAR=...`, a systemd
# `EnvironmentFile=`, docker-compose's `environment:`, etc.) for the running
# server to see it — copying this file to `.env` alone won't apply them.
#
# Nothing in this file is secret, but `.env` itself is gitignored anyway (a
# locally-added real secret should never risk being committed by habit).
# First-time setup: `cp .env.example .env`.

# --- Build-time: sqlx query macros ---

# Where `cargo sqlx prepare` / the `sqlx::query!` macros look for a database
# to check queries against at compile time. Must match whichever of
# edda-db's `sqlite` (default)/`postgres` Cargo features you're building
# with — the two are mutually exclusive (plan.local.md §17 Phase 3), so
# this only ever points at one backend at a time:
DATABASE_URL=sqlite://data/edda.db
# DATABASE_URL=postgres://edda:edda@localhost:5432/eddadb   # matches compose.db.yml, for `--features postgres` builds

# Skip the live-database requirement above and use the committed .sqlx/
# cache instead. Unset this (or override on the command line) when running
# `cargo sqlx prepare` after changing a query.
SQLX_OFFLINE=true

# --- Runtime: application ---

# Where repo data and the SQLite database live on disk. Only read by a
# `sqlite`-featured build.
# EDDA_DATA_DIR=./data

# Which database instance to connect to. Optional (and unused) on a
# `sqlite`-featured build — unset falls back to EDDA_DATA_DIR above,
# preserving the zero-config default. Required on a `postgres`-featured
# build (no local default exists for an external database):
# EDDA_DATABASE_URL=postgres://edda:edda@localhost:5432/eddadb

# Address the server binds to (read by dioxus_cli_config).
# IP=127.0.0.1
# PORT=8080

# --- Runtime: observability ---
# Structured local logging is always on and needs no configuration. Nothing
# below is required for the app to run normally — see README.md's
# "Observability" section for the full explanation of each variable.

# Setting any *_ENDPOINT below enables OTLP export. Point it at your
# OpenTelemetry Collector, never at a specific backend directly.
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=
# OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=

# Force OTel export off even if an endpoint above is set.
# OTEL_SDK_DISABLED=true

# Resource identity. OTEL_SERVICE_NAME defaults to "edda" when unset.
# OTEL_SERVICE_NAME=edda
# OTEL_SERVICE_VERSION=
# OTEL_RESOURCE_ATTRIBUTES=deployment.environment.name=production

# Trace sampling. Defaults when unset: always_on in debug builds,
# parentbased_traceidratio at 0.2 in release builds.
# OTEL_TRACES_SAMPLER=parentbased_traceidratio
# OTEL_TRACES_SAMPLER_ARG=0.2

# pretty|json — defaults to pretty in debug builds, json in release builds.
# EDDA_LOG_FORMAT=pretty
